<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/search"
        android:icon="@drawable/ic_search_24dp"
        android:title="@string/app_bar_bottom_search"
        android:contentDescription="@string/app_bar_bottom_content_description_search"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/more"
        android:contentDescription="@string/app_bar_bottom_content_description_more"
        android:title="@string/app_bar_bottom_more_info"
        app:showAsAction="never" />

</menu>